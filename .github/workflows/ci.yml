name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - uses: actions/checkout@v4

    - name: create env
      run: bin/hatch run dev-compiled:uv-pip-install

    - name: run pytest
      run: bin/hatch run dev-compiled:pytest
